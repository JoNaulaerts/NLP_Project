# Core framework
crewai==1.7.0
crewai-tools==1.7.0

# MCP client lib (your app talks to the gateway)
mcp==1.16.0
mcpadapt

# LangChain stack
langchain==1.2.0
langchain-core==1.2.1
langchain-community==0.4.1
langchain-text-splitters==1.1.0
langchain-ollama==1.0.0

# LiteLLM (keep your compatibility constraint)
litellm>=1.0.0,<1.70.0

# Vector DB client
chromadb==1.1.1

# Document processing
pypdf==6.4.1

# Web framework
streamlit==1.52.1

# Env/config + HTTP
python-dotenv==1.1.1
pydantic==2.11.10
httpx==0.28.1

# Embeddings / NLP
sentence-transformers==5.1.2

# Data
numpy>=1.26.2,<2.0.0           # Compatible with all dependencies (FIXED)
pandas==2.2.3
openpyxl==3.1.5                # For Excel/spreadsheet support

# Optional: only keep this if your code imports/uses it
mcp-proxy

# Document processing
python-docx==1.2.0             # Word documents
unstructured==0.16.9           # Multiple formats
filetype==1.2.0                # File type detection (cross-platform)
python-pptx==0.6.23            # PowerPoint support
